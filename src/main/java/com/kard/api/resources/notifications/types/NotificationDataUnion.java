/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.notifications.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class NotificationDataUnion {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private NotificationDataUnion(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static NotificationDataUnion earnedRewardApproved(EarnedRewardApprovedData value) {
        return new NotificationDataUnion(new EarnedRewardApprovedValue(value));
    }

    public static NotificationDataUnion earnedRewardSettled(EarnedRewardSettledData value) {
        return new NotificationDataUnion(new EarnedRewardSettledValue(value));
    }

    public static NotificationDataUnion validTransaction(ValidTransactionData value) {
        return new NotificationDataUnion(new ValidTransactionValue(value));
    }

    public static NotificationDataUnion failedTransaction(FailedTransactionData value) {
        return new NotificationDataUnion(new FailedTransactionValue(value));
    }

    public static NotificationDataUnion clawback(ClawbackData value) {
        return new NotificationDataUnion(new ClawbackValue(value));
    }

    public static NotificationDataUnion offer(WebhookOfferData value) {
        return new NotificationDataUnion(new OfferValue(value));
    }

    public static NotificationDataUnion merchant(WebhookMerchantData value) {
        return new NotificationDataUnion(new MerchantValue(value));
    }

    public static NotificationDataUnion location(WebhookLocationsData value) {
        return new NotificationDataUnion(new LocationValue(value));
    }

    public static NotificationDataUnion userOffer(WebhookUserOfferData value) {
        return new NotificationDataUnion(new UserOfferValue(value));
    }

    public static NotificationDataUnion auditUpdate(AuditUpdateData value) {
        return new NotificationDataUnion(new AuditUpdateValue(value));
    }

    public boolean isEarnedRewardApproved() {
        return value instanceof EarnedRewardApprovedValue;
    }

    public boolean isEarnedRewardSettled() {
        return value instanceof EarnedRewardSettledValue;
    }

    public boolean isValidTransaction() {
        return value instanceof ValidTransactionValue;
    }

    public boolean isFailedTransaction() {
        return value instanceof FailedTransactionValue;
    }

    public boolean isClawback() {
        return value instanceof ClawbackValue;
    }

    public boolean isOffer() {
        return value instanceof OfferValue;
    }

    public boolean isMerchant() {
        return value instanceof MerchantValue;
    }

    public boolean isLocation() {
        return value instanceof LocationValue;
    }

    public boolean isUserOffer() {
        return value instanceof UserOfferValue;
    }

    public boolean isAuditUpdate() {
        return value instanceof AuditUpdateValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<EarnedRewardApprovedData> getEarnedRewardApproved() {
        if (isEarnedRewardApproved()) {
            return Optional.of(((EarnedRewardApprovedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<EarnedRewardSettledData> getEarnedRewardSettled() {
        if (isEarnedRewardSettled()) {
            return Optional.of(((EarnedRewardSettledValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ValidTransactionData> getValidTransaction() {
        if (isValidTransaction()) {
            return Optional.of(((ValidTransactionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<FailedTransactionData> getFailedTransaction() {
        if (isFailedTransaction()) {
            return Optional.of(((FailedTransactionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ClawbackData> getClawback() {
        if (isClawback()) {
            return Optional.of(((ClawbackValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<WebhookOfferData> getOffer() {
        if (isOffer()) {
            return Optional.of(((OfferValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<WebhookMerchantData> getMerchant() {
        if (isMerchant()) {
            return Optional.of(((MerchantValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<WebhookLocationsData> getLocation() {
        if (isLocation()) {
            return Optional.of(((LocationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<WebhookUserOfferData> getUserOffer() {
        if (isUserOffer()) {
            return Optional.of(((UserOfferValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AuditUpdateData> getAuditUpdate() {
        if (isAuditUpdate()) {
            return Optional.of(((AuditUpdateValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitEarnedRewardApproved(EarnedRewardApprovedData earnedRewardApproved);

        T visitEarnedRewardSettled(EarnedRewardSettledData earnedRewardSettled);

        T visitValidTransaction(ValidTransactionData validTransaction);

        T visitFailedTransaction(FailedTransactionData failedTransaction);

        T visitClawback(ClawbackData clawback);

        T visitOffer(WebhookOfferData offer);

        T visitMerchant(WebhookMerchantData merchant);

        T visitLocation(WebhookLocationsData location);

        T visitUserOffer(WebhookUserOfferData userOffer);

        T visitAuditUpdate(AuditUpdateData auditUpdate);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(EarnedRewardApprovedValue.class),
        @JsonSubTypes.Type(EarnedRewardSettledValue.class),
        @JsonSubTypes.Type(ValidTransactionValue.class),
        @JsonSubTypes.Type(FailedTransactionValue.class),
        @JsonSubTypes.Type(ClawbackValue.class),
        @JsonSubTypes.Type(OfferValue.class),
        @JsonSubTypes.Type(MerchantValue.class),
        @JsonSubTypes.Type(LocationValue.class),
        @JsonSubTypes.Type(UserOfferValue.class),
        @JsonSubTypes.Type(AuditUpdateValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("earnedRewardApproved")
    @JsonIgnoreProperties("type")
    private static final class EarnedRewardApprovedValue implements Value {
        @JsonUnwrapped
        private EarnedRewardApprovedData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private EarnedRewardApprovedValue() {}

        private EarnedRewardApprovedValue(EarnedRewardApprovedData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitEarnedRewardApproved(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof EarnedRewardApprovedValue && equalTo((EarnedRewardApprovedValue) other);
        }

        private boolean equalTo(EarnedRewardApprovedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("earnedRewardSettled")
    @JsonIgnoreProperties("type")
    private static final class EarnedRewardSettledValue implements Value {
        @JsonUnwrapped
        private EarnedRewardSettledData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private EarnedRewardSettledValue() {}

        private EarnedRewardSettledValue(EarnedRewardSettledData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitEarnedRewardSettled(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof EarnedRewardSettledValue && equalTo((EarnedRewardSettledValue) other);
        }

        private boolean equalTo(EarnedRewardSettledValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("validTransaction")
    @JsonIgnoreProperties("type")
    private static final class ValidTransactionValue implements Value {
        @JsonUnwrapped
        private ValidTransactionData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ValidTransactionValue() {}

        private ValidTransactionValue(ValidTransactionData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitValidTransaction(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ValidTransactionValue && equalTo((ValidTransactionValue) other);
        }

        private boolean equalTo(ValidTransactionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("failedTransaction")
    @JsonIgnoreProperties("type")
    private static final class FailedTransactionValue implements Value {
        @JsonUnwrapped
        private FailedTransactionData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private FailedTransactionValue() {}

        private FailedTransactionValue(FailedTransactionData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitFailedTransaction(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof FailedTransactionValue && equalTo((FailedTransactionValue) other);
        }

        private boolean equalTo(FailedTransactionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("clawback")
    @JsonIgnoreProperties("type")
    private static final class ClawbackValue implements Value {
        @JsonUnwrapped
        private ClawbackData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ClawbackValue() {}

        private ClawbackValue(ClawbackData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitClawback(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ClawbackValue && equalTo((ClawbackValue) other);
        }

        private boolean equalTo(ClawbackValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("offer")
    @JsonIgnoreProperties("type")
    private static final class OfferValue implements Value {
        @JsonUnwrapped
        private WebhookOfferData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private OfferValue() {}

        private OfferValue(WebhookOfferData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitOffer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof OfferValue && equalTo((OfferValue) other);
        }

        private boolean equalTo(OfferValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("merchant")
    @JsonIgnoreProperties("type")
    private static final class MerchantValue implements Value {
        @JsonUnwrapped
        private WebhookMerchantData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MerchantValue() {}

        private MerchantValue(WebhookMerchantData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMerchant(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MerchantValue && equalTo((MerchantValue) other);
        }

        private boolean equalTo(MerchantValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("location")
    @JsonIgnoreProperties("type")
    private static final class LocationValue implements Value {
        @JsonUnwrapped
        private WebhookLocationsData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private LocationValue() {}

        private LocationValue(WebhookLocationsData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitLocation(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof LocationValue && equalTo((LocationValue) other);
        }

        private boolean equalTo(LocationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("userOffer")
    @JsonIgnoreProperties("type")
    private static final class UserOfferValue implements Value {
        @JsonUnwrapped
        private WebhookUserOfferData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UserOfferValue() {}

        private UserOfferValue(WebhookUserOfferData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUserOffer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UserOfferValue && equalTo((UserOfferValue) other);
        }

        private boolean equalTo(UserOfferValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("auditUpdate")
    @JsonIgnoreProperties("type")
    private static final class AuditUpdateValue implements Value {
        @JsonUnwrapped
        private AuditUpdateData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AuditUpdateValue() {}

        private AuditUpdateValue(AuditUpdateData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAuditUpdate(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AuditUpdateValue && equalTo((AuditUpdateValue) other);
        }

        private boolean equalTo(AuditUpdateValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NotificationDataUnion{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
