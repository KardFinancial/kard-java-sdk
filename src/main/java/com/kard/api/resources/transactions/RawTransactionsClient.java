/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.transactions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.kard.api.core.ClientOptions;
import com.kard.api.core.KardApiApiException;
import com.kard.api.core.KardApiException;
import com.kard.api.core.KardApiHttpResponse;
import com.kard.api.core.MediaTypes;
import com.kard.api.core.ObjectMappers;
import com.kard.api.core.QueryStringMapper;
import com.kard.api.core.RequestOptions;
import com.kard.api.resources.commons.errors.ConflictError;
import com.kard.api.resources.commons.errors.DoesNotExistError;
import com.kard.api.resources.commons.errors.InternalServerError;
import com.kard.api.resources.commons.errors.InvalidRequest;
import com.kard.api.resources.commons.errors.UnauthorizedError;
import com.kard.api.resources.commons.types.ErrorResponse;
import com.kard.api.resources.transactions.errors.CreateAuditMultiStatus;
import com.kard.api.resources.transactions.errors.CreateIncomingTransactionsMultiStatus;
import com.kard.api.resources.transactions.errors.FraudMultiStatus;
import com.kard.api.resources.transactions.requests.GetEarnedRewardsRequest;
import com.kard.api.resources.transactions.types.CreateAuditMultiStatusResponse;
import com.kard.api.resources.transactions.types.CreateAuditRequestBody;
import com.kard.api.resources.transactions.types.CreateAuditResponseBody;
import com.kard.api.resources.transactions.types.FraudulentTransactionObject;
import com.kard.api.resources.transactions.types.FraudulentTransactionRequestBody;
import com.kard.api.resources.transactions.types.FraudulentTransactionResponse;
import com.kard.api.resources.transactions.types.GetEarnedRewardsResponse;
import com.kard.api.resources.transactions.types.TransactionsMultiResponse;
import com.kard.api.resources.transactions.types.TransactionsRequestBody;
import com.kard.api.resources.transactions.types.TransactionsResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawTransactionsClient {
    protected final ClientOptions clientOptions;

    public RawTransactionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Call this endpoint to send all transactions made by all your enrolled users in your rewards program. The request body will depend on the transaction type.&lt;br/&gt;
     * Please use the correct type when calling the endpoint:
     * <ul>
     * <li><code>transaction</code>: These incoming transactions will be processed and matched by the Kard system. Learn more about the <a href="https://github.com/kard-financial/kard-postman#c-transaction-clo-matching">Transaction CLO Matching</a> flow here.</li>
     * <li><code>matchedTransaction</code>: For pre-matched transactions that need validation on match by the Kard system.&lt;br/&gt;</li>
     * </ul>
     * <p>&lt;b&gt;Required scopes:&lt;/b&gt; <code>transaction:write</code>&lt;br/&gt;
     * &lt;b&gt;Note:&lt;/b&gt; <code>Maximum of 500 transactions can be created per request</code>.</p>
     */
    public KardApiHttpResponse<TransactionsResponse> create(String organizationId, TransactionsRequestBody request) {
        return create(organizationId, request, null);
    }

    /**
     * Call this endpoint to send all transactions made by all your enrolled users in your rewards program. The request body will depend on the transaction type.&lt;br/&gt;
     * Please use the correct type when calling the endpoint:
     * <ul>
     * <li><code>transaction</code>: These incoming transactions will be processed and matched by the Kard system. Learn more about the <a href="https://github.com/kard-financial/kard-postman#c-transaction-clo-matching">Transaction CLO Matching</a> flow here.</li>
     * <li><code>matchedTransaction</code>: For pre-matched transactions that need validation on match by the Kard system.&lt;br/&gt;</li>
     * </ul>
     * <p>&lt;b&gt;Required scopes:&lt;/b&gt; <code>transaction:write</code>&lt;br/&gt;
     * &lt;b&gt;Note:&lt;/b&gt; <code>Maximum of 500 transactions can be created per request</code>.</p>
     */
    public KardApiHttpResponse<TransactionsResponse> create(
            String organizationId, TransactionsRequestBody request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("transactions")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TransactionsResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 207:
                        throw new CreateIncomingTransactionsMultiStatus(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, TransactionsMultiResponse.class),
                                response);
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to flag a submitted transaction as fraudulent. This will prevent it from being rewarded.&lt;br/&gt;
     * <p>&lt;b&gt;Required scopes:&lt;/b&gt;  <code>transaction:write</code>&lt;br/&gt;
     * &lt;b&gt;Note:&lt;/b&gt; <code>Maximum of 500 fraudulent transactions can be created per request</code>.</p>
     */
    public KardApiHttpResponse<FraudulentTransactionObject> createFraudMarkers(
            String organizationId, FraudulentTransactionRequestBody request) {
        return createFraudMarkers(organizationId, request, null);
    }

    /**
     * Call this endpoint to flag a submitted transaction as fraudulent. This will prevent it from being rewarded.&lt;br/&gt;
     * <p>&lt;b&gt;Required scopes:&lt;/b&gt;  <code>transaction:write</code>&lt;br/&gt;
     * &lt;b&gt;Note:&lt;/b&gt; <code>Maximum of 500 fraudulent transactions can be created per request</code>.</p>
     */
    public KardApiHttpResponse<FraudulentTransactionObject> createFraudMarkers(
            String organizationId, FraudulentTransactionRequestBody request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("fraud")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FraudulentTransactionObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 207:
                        throw new FraudMultiStatus(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, FraudulentTransactionResponse.class),
                                response);
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to request that a particular transaction be audited further by the Kard system, in the event of a missing cashback claim, incorrect cashback amount claim or other mis-match claims.&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>audit:write</code>
     */
    public KardApiHttpResponse<CreateAuditResponseBody> createAudits(
            String organizationId, String userId, CreateAuditRequestBody request) {
        return createAudits(organizationId, userId, request, null);
    }

    /**
     * Call this endpoint to request that a particular transaction be audited further by the Kard system, in the event of a missing cashback claim, incorrect cashback amount claim or other mis-match claims.&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>audit:write</code>
     */
    public KardApiHttpResponse<CreateAuditResponseBody> createAudits(
            String organizationId, String userId, CreateAuditRequestBody request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegment(userId)
                .addPathSegments("audits")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateAuditResponseBody.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 207:
                        throw new CreateAuditMultiStatus(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, CreateAuditMultiStatusResponse.class),
                                response);
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieve rewarded transaction history for a specific user. Returns only SETTLED transactions within the last 12 months.
     * &lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>transaction:read</code>
     * &lt;br/&gt;
     * &lt;b&gt;Query Limit:&lt;/b&gt; Maximum of 12 months of transaction data can be queried.
     */
    public KardApiHttpResponse<GetEarnedRewardsResponse> getEarnedRewards(String organizationId, String userId) {
        return getEarnedRewards(
                organizationId, userId, GetEarnedRewardsRequest.builder().build());
    }

    /**
     * Retrieve rewarded transaction history for a specific user. Returns only SETTLED transactions within the last 12 months.
     * &lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>transaction:read</code>
     * &lt;br/&gt;
     * &lt;b&gt;Query Limit:&lt;/b&gt; Maximum of 12 months of transaction data can be queried.
     */
    public KardApiHttpResponse<GetEarnedRewardsResponse> getEarnedRewards(
            String organizationId, String userId, GetEarnedRewardsRequest request) {
        return getEarnedRewards(organizationId, userId, request, null);
    }

    /**
     * Retrieve rewarded transaction history for a specific user. Returns only SETTLED transactions within the last 12 months.
     * &lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>transaction:read</code>
     * &lt;br/&gt;
     * &lt;b&gt;Query Limit:&lt;/b&gt; Maximum of 12 months of transaction data can be queried.
     */
    public KardApiHttpResponse<GetEarnedRewardsResponse> getEarnedRewards(
            String organizationId, String userId, GetEarnedRewardsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegment(userId)
                .addPathSegments("earned-rewards");
        if (request.getPageAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[after]", request.getPageAfter().get(), false);
        }
        if (request.getPageBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[before]", request.getPageBefore().get(), false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[size]", request.getPageSize().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetEarnedRewardsResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }
}
