/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.attributions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.kard.api.core.ClientOptions;
import com.kard.api.core.KardApiApiException;
import com.kard.api.core.KardApiException;
import com.kard.api.core.KardApiHttpResponse;
import com.kard.api.core.MediaTypes;
import com.kard.api.core.ObjectMappers;
import com.kard.api.core.QueryStringMapper;
import com.kard.api.core.RequestOptions;
import com.kard.api.resources.attributions.requests.InternalGetAttributionsRequest;
import com.kard.api.resources.attributions.types.InternalBulkCreateAttributionsRequest;
import com.kard.api.resources.attributions.types.InternalBulkCreateAttributionsResponse;
import com.kard.api.resources.attributions.types.InternalGetAttributionsResponse;
import com.kard.api.resources.commons.errors.InternalServerError;
import com.kard.api.resources.commons.errors.InvalidRequest;
import com.kard.api.resources.commons.errors.UnauthorizedError;
import com.kard.api.resources.commons.types.ErrorResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawAttributionsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawAttributionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Call this endpoint to persist attribution events. A maximum of 100 events can be included in a single request.
     */
    public CompletableFuture<KardApiHttpResponse<InternalBulkCreateAttributionsResponse>>
            internalBulkCreateAttributions(InternalBulkCreateAttributionsRequest request) {
        return internalBulkCreateAttributions(request, null);
    }

    /**
     * Call this endpoint to persist attribution events. A maximum of 100 events can be included in a single request.
     */
    public CompletableFuture<KardApiHttpResponse<InternalBulkCreateAttributionsResponse>>
            internalBulkCreateAttributions(
                    InternalBulkCreateAttributionsRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/internal/attributions")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<KardApiHttpResponse<InternalBulkCreateAttributionsResponse>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new KardApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, InternalBulkCreateAttributionsResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new InvalidRequest(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new KardApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Retrieve a list of attribution records with optional filtering and pagination.
     * Returns attribution events that match the specified criteria.
     */
    public CompletableFuture<KardApiHttpResponse<InternalGetAttributionsResponse>> internalGetAttributions(
            String organizationId, String userId) {
        return internalGetAttributions(
                organizationId, userId, InternalGetAttributionsRequest.builder().build());
    }

    /**
     * Retrieve a list of attribution records with optional filtering and pagination.
     * Returns attribution events that match the specified criteria.
     */
    public CompletableFuture<KardApiHttpResponse<InternalGetAttributionsResponse>> internalGetAttributions(
            String organizationId, String userId, InternalGetAttributionsRequest request) {
        return internalGetAttributions(organizationId, userId, request, null);
    }

    /**
     * Retrieve a list of attribution records with optional filtering and pagination.
     * Returns attribution events that match the specified criteria.
     */
    public CompletableFuture<KardApiHttpResponse<InternalGetAttributionsResponse>> internalGetAttributions(
            String organizationId,
            String userId,
            InternalGetAttributionsRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/internal/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegment(userId)
                .addPathSegments("attributions");
        if (request.getFilterType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[type]", request.getFilterType().get(), false);
        }
        if (request.getFilterEntityId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[entityId]", request.getFilterEntityId().get(), false);
        }
        if (request.getFilterEventCode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[eventCode]", request.getFilterEventCode().get(), false);
        }
        if (request.getFilterStartDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[startDate]", request.getFilterStartDate().get(), false);
        }
        if (request.getFilterEndDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[endDate]", request.getFilterEndDate().get(), false);
        }
        if (request.getPageAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[after]", request.getPageAfter().get(), false);
        }
        if (request.getPageBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[before]", request.getPageBefore().get(), false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[size]", request.getPageSize().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<KardApiHttpResponse<InternalGetAttributionsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new KardApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, InternalGetAttributionsResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new InvalidRequest(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new KardApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
