/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.users.rewards;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.kard.api.core.ClientOptions;
import com.kard.api.core.KardApiApiException;
import com.kard.api.core.KardApiException;
import com.kard.api.core.KardApiHttpResponse;
import com.kard.api.core.ObjectMappers;
import com.kard.api.core.QueryStringMapper;
import com.kard.api.core.RequestOptions;
import com.kard.api.resources.commons.errors.DoesNotExistError;
import com.kard.api.resources.commons.errors.InternalServerError;
import com.kard.api.resources.commons.errors.InvalidRequest;
import com.kard.api.resources.commons.errors.UnauthorizedError;
import com.kard.api.resources.commons.types.ErrorResponse;
import com.kard.api.resources.users.rewards.requests.GetLocationsByUserRequest;
import com.kard.api.resources.users.rewards.requests.GetOffersByUserRequest;
import com.kard.api.resources.users.rewards.types.LocationsResponseObject;
import com.kard.api.resources.users.rewards.types.OffersResponseObject;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawRewardsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawRewardsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Retrieve national brand offers that a specified user is eligible for. Call this endpoint to build out your
     * <a href="/2024-10-01/api/getting-started#b-discover-a-lapsed-customer-clo">targeted offers UX experience</a>. Local offers details
     * can be found by calling the <a href="/2024-10-01/api/rewards/locations">Get Eligible Locations</a>.<br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<OffersResponseObject>> offers(String organizationId, String userId) {
        return offers(organizationId, userId, GetOffersByUserRequest.builder().build());
    }

    /**
     * Retrieve national brand offers that a specified user is eligible for. Call this endpoint to build out your
     * <a href="/2024-10-01/api/getting-started#b-discover-a-lapsed-customer-clo">targeted offers UX experience</a>. Local offers details
     * can be found by calling the <a href="/2024-10-01/api/rewards/locations">Get Eligible Locations</a>.<br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<OffersResponseObject>> offers(
            String organizationId, String userId, RequestOptions requestOptions) {
        return offers(organizationId, userId, GetOffersByUserRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieve national brand offers that a specified user is eligible for. Call this endpoint to build out your
     * <a href="/2024-10-01/api/getting-started#b-discover-a-lapsed-customer-clo">targeted offers UX experience</a>. Local offers details
     * can be found by calling the <a href="/2024-10-01/api/rewards/locations">Get Eligible Locations</a>.<br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<OffersResponseObject>> offers(
            String organizationId, String userId, GetOffersByUserRequest request) {
        return offers(organizationId, userId, request, null);
    }

    /**
     * Retrieve national brand offers that a specified user is eligible for. Call this endpoint to build out your
     * <a href="/2024-10-01/api/getting-started#b-discover-a-lapsed-customer-clo">targeted offers UX experience</a>. Local offers details
     * can be found by calling the <a href="/2024-10-01/api/rewards/locations">Get Eligible Locations</a>.<br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<OffersResponseObject>> offers(
            String organizationId, String userId, GetOffersByUserRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegment(userId)
                .addPathSegments("offers");
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[size]", request.getPageSize().get(), false);
        }
        if (request.getPageAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[after]", request.getPageAfter().get(), false);
        }
        if (request.getPageBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[before]", request.getPageBefore().get(), false);
        }
        if (request.getFilterPurchaseChannel().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "filter[purchaseChannel]",
                    request.getFilterPurchaseChannel().get(),
                    false);
        }
        if (request.getFilterCategory().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[category]", request.getFilterCategory().get(), false);
        }
        if (request.getFilterIsTargeted().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[isTargeted]", request.getFilterIsTargeted().get(), false);
        }
        if (request.getSort().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sort", request.getSort().get(), true);
        }
        if (request.getInclude().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "include", request.getInclude().get(), true);
        }
        if (request.getSupportedComponents().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "supportedComponents",
                    request.getSupportedComponents().get(),
                    true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<KardApiHttpResponse<OffersResponseObject>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new KardApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OffersResponseObject.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new InvalidRequest(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new DoesNotExistError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new KardApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Retrieve national and local geographic locations that a specified user has eligible in-store offers at. Use this endpoint to build
     * out your <a href="/2024-10-01/api/getting-started#c-discover-clos-near-you-map-view">map-specific UX experiences</a>. Please note
     * that Longitude and Latitude fields are prioritized over State, City and Zipcode and are the recommended search
     * pattern.<br/>
     * <br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<LocationsResponseObject>> locations(
            String organizationId, String userId) {
        return locations(
                organizationId, userId, GetLocationsByUserRequest.builder().build());
    }

    /**
     * Retrieve national and local geographic locations that a specified user has eligible in-store offers at. Use this endpoint to build
     * out your <a href="/2024-10-01/api/getting-started#c-discover-clos-near-you-map-view">map-specific UX experiences</a>. Please note
     * that Longitude and Latitude fields are prioritized over State, City and Zipcode and are the recommended search
     * pattern.<br/>
     * <br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<LocationsResponseObject>> locations(
            String organizationId, String userId, RequestOptions requestOptions) {
        return locations(
                organizationId, userId, GetLocationsByUserRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieve national and local geographic locations that a specified user has eligible in-store offers at. Use this endpoint to build
     * out your <a href="/2024-10-01/api/getting-started#c-discover-clos-near-you-map-view">map-specific UX experiences</a>. Please note
     * that Longitude and Latitude fields are prioritized over State, City and Zipcode and are the recommended search
     * pattern.<br/>
     * <br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<LocationsResponseObject>> locations(
            String organizationId, String userId, GetLocationsByUserRequest request) {
        return locations(organizationId, userId, request, null);
    }

    /**
     * Retrieve national and local geographic locations that a specified user has eligible in-store offers at. Use this endpoint to build
     * out your <a href="/2024-10-01/api/getting-started#c-discover-clos-near-you-map-view">map-specific UX experiences</a>. Please note
     * that Longitude and Latitude fields are prioritized over State, City and Zipcode and are the recommended search
     * pattern.<br/>
     * <br/>
     * <b>Required scopes:</b> <code>rewards:read</code>
     */
    public CompletableFuture<KardApiHttpResponse<LocationsResponseObject>> locations(
            String organizationId, String userId, GetLocationsByUserRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("users")
                .addPathSegment(userId)
                .addPathSegments("locations");
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[size]", request.getPageSize().get(), false);
        }
        if (request.getPageAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[after]", request.getPageAfter().get(), false);
        }
        if (request.getPageBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[before]", request.getPageBefore().get(), false);
        }
        if (request.getFilterName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[name]", request.getFilterName().get(), false);
        }
        if (request.getFilterCity().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[city]", request.getFilterCity().get(), false);
        }
        if (request.getFilterZipCode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[zipCode]", request.getFilterZipCode().get(), false);
        }
        if (request.getFilterState().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[state]", request.getFilterState().get(), false);
        }
        if (request.getFilterCategory().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[category]", request.getFilterCategory().get(), false);
        }
        if (request.getFilterLongitude().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[longitude]", request.getFilterLongitude().get(), false);
        }
        if (request.getFilterLatitude().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[latitude]", request.getFilterLatitude().get(), false);
        }
        if (request.getFilterRadius().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[radius]", request.getFilterRadius().get(), false);
        }
        if (request.getSort().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sort", request.getSort().get(), true);
        }
        if (request.getInclude().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "include", request.getInclude().get(), true);
        }
        if (request.getSupportedComponents().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "supportedComponents",
                    request.getSupportedComponents().get(),
                    true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<KardApiHttpResponse<LocationsResponseObject>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new KardApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LocationsResponseObject.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new InvalidRequest(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new DoesNotExistError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new KardApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new KardApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
