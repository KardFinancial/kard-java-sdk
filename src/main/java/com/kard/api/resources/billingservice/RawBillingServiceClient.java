/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.billingservice;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.kard.api.core.ClientOptions;
import com.kard.api.core.KardApiApiException;
import com.kard.api.core.KardApiException;
import com.kard.api.core.KardApiHttpResponse;
import com.kard.api.core.MediaTypes;
import com.kard.api.core.ObjectMappers;
import com.kard.api.core.QueryStringMapper;
import com.kard.api.core.RequestOptions;
import com.kard.api.resources.billingservice.requests.GetRewardEntriesRequest;
import com.kard.api.resources.billingservice.requests.GetRewardRequest;
import com.kard.api.resources.billingservice.requests.GetRewardsRequest;
import com.kard.api.resources.billingservice.requests.RevertRewardsRequest;
import com.kard.api.resources.billingservice.types.CreateRewardRequestBody;
import com.kard.api.resources.billingservice.types.CreateRewardResponseObject;
import com.kard.api.resources.billingservice.types.GetRewardEntriesResponseObject;
import com.kard.api.resources.billingservice.types.GetRewardResponseObject;
import com.kard.api.resources.billingservice.types.GetRewardsResponseObject;
import com.kard.api.resources.billingservice.types.InvoiceStatusFileResponseObject;
import com.kard.api.resources.billingservice.types.RevertRewardsResponseObject;
import com.kard.api.resources.billingservice.types.TriggerMerchantInvoiceRequestBody;
import com.kard.api.resources.billingservice.types.TriggerMerchantInvoiceResponseObject;
import com.kard.api.resources.billingservice.types.UpdateRewardRequestBody;
import com.kard.api.resources.billingservice.types.UpdateRewardResponseObject;
import com.kard.api.resources.commons.errors.DoesNotExistError;
import com.kard.api.resources.commons.errors.InternalServerError;
import com.kard.api.resources.commons.errors.InvalidRequest;
import com.kard.api.resources.commons.errors.UnauthorizedError;
import com.kard.api.resources.commons.types.ErrorResponse;
import java.io.IOException;
import java.util.UUID;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawBillingServiceClient {
    protected final ClientOptions clientOptions;

    public RawBillingServiceClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Call this endpoint to create a new reward entry in the ledger.&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:write</code>
     */
    public KardApiHttpResponse<CreateRewardResponseObject> createReward(CreateRewardRequestBody request) {
        return createReward(request, null);
    }

    /**
     * Call this endpoint to create a new reward entry in the ledger.&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:write</code>
     */
    public KardApiHttpResponse<CreateRewardResponseObject> createReward(
            CreateRewardRequestBody request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("rewards")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateRewardResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to get a reward by its ID.
     */
    public KardApiHttpResponse<GetRewardResponseObject> getReward(UUID rewardId) {
        return getReward(rewardId, GetRewardRequest.builder().build());
    }

    /**
     * Call this endpoint to get a reward by its ID.
     */
    public KardApiHttpResponse<GetRewardResponseObject> getReward(UUID rewardId, GetRewardRequest request) {
        return getReward(rewardId, request, null);
    }

    /**
     * Call this endpoint to get a reward by its ID.
     */
    public KardApiHttpResponse<GetRewardResponseObject> getReward(
            UUID rewardId, GetRewardRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("rewards")
                .addPathSegment(rewardId.toString());
        if (request.getEntries().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "entries", request.getEntries().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetRewardResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to list rewards. MUST BE FILTERED&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:read</code>
     */
    public KardApiHttpResponse<GetRewardsResponseObject> getRewards() {
        return getRewards(GetRewardsRequest.builder().build());
    }

    /**
     * Call this endpoint to list rewards. MUST BE FILTERED&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:read</code>
     */
    public KardApiHttpResponse<GetRewardsResponseObject> getRewards(GetRewardsRequest request) {
        return getRewards(request, null);
    }

    /**
     * Call this endpoint to list rewards. MUST BE FILTERED&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:read</code>
     */
    public KardApiHttpResponse<GetRewardsResponseObject> getRewards(
            GetRewardsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("rewards");
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getFilter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter", request.getFilter().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetRewardsResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to list reward entries.&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:read</code>
     */
    public KardApiHttpResponse<GetRewardEntriesResponseObject> getRewardEntries() {
        return getRewardEntries(GetRewardEntriesRequest.builder().build());
    }

    /**
     * Call this endpoint to list reward entries.&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:read</code>
     */
    public KardApiHttpResponse<GetRewardEntriesResponseObject> getRewardEntries(GetRewardEntriesRequest request) {
        return getRewardEntries(request, null);
    }

    /**
     * Call this endpoint to list reward entries.&lt;br/&gt;
     * &lt;b&gt;Required scopes:&lt;/b&gt; <code>rewards:read</code>
     */
    public KardApiHttpResponse<GetRewardEntriesResponseObject> getRewardEntries(
            GetRewardEntriesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("rewards/entries");
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getFilter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter", request.getFilter().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetRewardEntriesResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to create an adjustment entry in the ledger for a reward.
     */
    public KardApiHttpResponse<UpdateRewardResponseObject> updateReward(
            UUID rewardId, UpdateRewardRequestBody request) {
        return updateReward(rewardId, request, null);
    }

    /**
     * Call this endpoint to create an adjustment entry in the ledger for a reward.
     */
    public KardApiHttpResponse<UpdateRewardResponseObject> updateReward(
            UUID rewardId, UpdateRewardRequestBody request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("rewards")
                .addPathSegment(rewardId.toString())
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateRewardResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to revert a reward
     */
    public KardApiHttpResponse<RevertRewardsResponseObject> revertReward(UUID rewardId) {
        return revertReward(rewardId, null);
    }

    /**
     * Call this endpoint to revert a reward
     */
    public KardApiHttpResponse<RevertRewardsResponseObject> revertReward(UUID rewardId, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("rewards")
                .addPathSegment(rewardId.toString())
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RevertRewardsResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint to revert rewards
     */
    public KardApiHttpResponse<RevertRewardsResponseObject> revertRewards() {
        return revertRewards(RevertRewardsRequest.builder().build());
    }

    /**
     * Call this endpoint to revert rewards
     */
    public KardApiHttpResponse<RevertRewardsResponseObject> revertRewards(RevertRewardsRequest request) {
        return revertRewards(request, null);
    }

    /**
     * Call this endpoint to revert rewards
     */
    public KardApiHttpResponse<RevertRewardsResponseObject> revertRewards(
            RevertRewardsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("rewards");
        if (request.getFilter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter", request.getFilter().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RevertRewardsResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Call this endpoint get a presigned URL that can be used to upload the EOM invoice status file.
     */
    public KardApiHttpResponse<InvoiceStatusFileResponseObject> uploadInvoiceStatus() {
        return uploadInvoiceStatus(null);
    }

    /**
     * Call this endpoint get a presigned URL that can be used to upload the EOM invoice status file.
     */
    public KardApiHttpResponse<InvoiceStatusFileResponseObject> uploadInvoiceStatus(RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("invoice/status/upload")
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvoiceStatusFileResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Triggers the job to issue a merchant invoice
     */
    public KardApiHttpResponse<TriggerMerchantInvoiceResponseObject> triggerMerchantInvoice(
            String merchantId, TriggerMerchantInvoiceRequestBody request) {
        return triggerMerchantInvoice(merchantId, request, null);
    }

    /**
     * Triggers the job to issue a merchant invoice
     */
    public KardApiHttpResponse<TriggerMerchantInvoiceResponseObject> triggerMerchantInvoice(
            String merchantId, TriggerMerchantInvoiceRequestBody request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/billing")
                .addPathSegments("merchants")
                .addPathSegment(merchantId)
                .addPathSegments("invoices")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, TriggerMerchantInvoiceResponseObject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }
}
