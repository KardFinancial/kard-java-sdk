/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.organizations.contentstrategies;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.kard.api.core.ClientOptions;
import com.kard.api.core.KardApiApiException;
import com.kard.api.core.KardApiException;
import com.kard.api.core.KardApiHttpResponse;
import com.kard.api.core.MediaTypes;
import com.kard.api.core.ObjectMappers;
import com.kard.api.core.QueryStringMapper;
import com.kard.api.core.RequestOptions;
import com.kard.api.resources.commons.errors.ConflictError;
import com.kard.api.resources.commons.errors.DoesNotExistError;
import com.kard.api.resources.commons.errors.ForbiddenError;
import com.kard.api.resources.commons.errors.InternalServerError;
import com.kard.api.resources.commons.errors.InvalidRequest;
import com.kard.api.resources.commons.errors.UnauthorizedError;
import com.kard.api.resources.commons.types.ErrorResponse;
import com.kard.api.resources.internalorganizations.types.DeleteResourceResponse;
import com.kard.api.resources.organizations.contentstrategies.requests.ListContentStrategiesRequest;
import com.kard.api.resources.organizations.contentstrategies.types.ContentStrategyListResponse;
import com.kard.api.resources.organizations.contentstrategies.types.ContentStrategyResponse;
import com.kard.api.resources.organizations.contentstrategies.types.CreateContentStrategyRequestBody;
import com.kard.api.resources.organizations.contentstrategies.types.UpdateContentStrategyRequestBody;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawContentStrategiesClient {
    protected final ClientOptions clientOptions;

    public RawContentStrategiesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Create a content strategy for the organization. The strategy name must be unique within the organization.
     */
    public KardApiHttpResponse<ContentStrategyResponse> create(
            String organizationId, CreateContentStrategyRequestBody request) {
        return create(organizationId, request, null);
    }

    /**
     * Create a content strategy for the organization. The strategy name must be unique within the organization.
     */
    public KardApiHttpResponse<ContentStrategyResponse> create(
            String organizationId, CreateContentStrategyRequestBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("contentStrategies");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ContentStrategyResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * List content strategies belonging to the authenticated organization
     */
    public KardApiHttpResponse<ContentStrategyListResponse> list(String organizationId) {
        return list(organizationId, ListContentStrategiesRequest.builder().build());
    }

    /**
     * List content strategies belonging to the authenticated organization
     */
    public KardApiHttpResponse<ContentStrategyListResponse> list(String organizationId, RequestOptions requestOptions) {
        return list(organizationId, ListContentStrategiesRequest.builder().build(), requestOptions);
    }

    /**
     * List content strategies belonging to the authenticated organization
     */
    public KardApiHttpResponse<ContentStrategyListResponse> list(
            String organizationId, ListContentStrategiesRequest request) {
        return list(organizationId, request, null);
    }

    /**
     * List content strategies belonging to the authenticated organization
     */
    public KardApiHttpResponse<ContentStrategyListResponse> list(
            String organizationId, ListContentStrategiesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("contentStrategies");
        if (request.getFilterName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filter[name]", request.getFilterName().get(), false);
        }
        if (request.getPageAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[after]", request.getPageAfter().get(), false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page[size]", request.getPageSize().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ContentStrategyListResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieve a specific content strategy
     */
    public KardApiHttpResponse<ContentStrategyResponse> get(String organizationId, String contentStrategyId) {
        return get(organizationId, contentStrategyId, null);
    }

    /**
     * Retrieve a specific content strategy
     */
    public KardApiHttpResponse<ContentStrategyResponse> get(
            String organizationId, String contentStrategyId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("contentStrategies")
                .addPathSegment(contentStrategyId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ContentStrategyResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Replace a content strategy. All fields must be provided; any omitted attribute is treated as cleared.
     */
    public KardApiHttpResponse<ContentStrategyResponse> update(
            String organizationId, String contentStrategyId, UpdateContentStrategyRequestBody request) {
        return update(organizationId, contentStrategyId, request, null);
    }

    /**
     * Replace a content strategy. All fields must be provided; any omitted attribute is treated as cleared.
     */
    public KardApiHttpResponse<ContentStrategyResponse> update(
            String organizationId,
            String contentStrategyId,
            UpdateContentStrategyRequestBody request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("contentStrategies")
                .addPathSegment(contentStrategyId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new KardApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ContentStrategyResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new InvalidRequest(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Delete a content strategy. Returns 409 if the strategy is still referenced by another resource.
     */
    public KardApiHttpResponse<DeleteResourceResponse> delete(String organizationId, String contentStrategyId) {
        return delete(organizationId, contentStrategyId, null);
    }

    /**
     * Delete a content strategy. Returns 409 if the strategy is still referenced by another resource.
     */
    public KardApiHttpResponse<DeleteResourceResponse> delete(
            String organizationId, String contentStrategyId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/issuers")
                .addPathSegment(organizationId)
                .addPathSegments("contentStrategies")
                .addPathSegment(contentStrategyId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new KardApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteResourceResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new DoesNotExistError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new KardApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new KardApiException("Network error executing HTTP request", e);
        }
    }
}
