/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.organizations.placements.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class PlacementStatus {
    public static final PlacementStatus ACTIVE = new PlacementStatus(Value.ACTIVE, "ACTIVE");

    public static final PlacementStatus INACTIVE = new PlacementStatus(Value.INACTIVE, "INACTIVE");

    private final Value value;

    private final String string;

    PlacementStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof PlacementStatus && this.string.equals(((PlacementStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case ACTIVE:
                return visitor.visitActive();
            case INACTIVE:
                return visitor.visitInactive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static PlacementStatus valueOf(String value) {
        switch (value) {
            case "ACTIVE":
                return ACTIVE;
            case "INACTIVE":
                return INACTIVE;
            default:
                return new PlacementStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        INACTIVE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitInactive();

        T visitUnknown(String unknownType);
    }
}
