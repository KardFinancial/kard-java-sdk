/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.organizations.placements.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class IncludedResource {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private IncludedResource(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static IncludedResource contentStrategy(ContentStrategyInclusion value) {
        return new IncludedResource(new ContentStrategyValue(value));
    }

    public static IncludedResource batchActivationSlot(BatchActivationSlotInclusion value) {
        return new IncludedResource(new BatchActivationSlotValue(value));
    }

    public static IncludedResource placement(PlacementData value) {
        return new IncludedResource(new PlacementValue(value));
    }

    public static IncludedResource placementPushNotification(PushNotificationPlacementData value) {
        return new IncludedResource(new PlacementPushNotificationValue(value));
    }

    public static IncludedResource placementEmail(EmailPlacementData value) {
        return new IncludedResource(new PlacementEmailValue(value));
    }

    public boolean isContentStrategy() {
        return value instanceof ContentStrategyValue;
    }

    public boolean isBatchActivationSlot() {
        return value instanceof BatchActivationSlotValue;
    }

    public boolean isPlacement() {
        return value instanceof PlacementValue;
    }

    public boolean isPlacementPushNotification() {
        return value instanceof PlacementPushNotificationValue;
    }

    public boolean isPlacementEmail() {
        return value instanceof PlacementEmailValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<ContentStrategyInclusion> getContentStrategy() {
        if (isContentStrategy()) {
            return Optional.of(((ContentStrategyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<BatchActivationSlotInclusion> getBatchActivationSlot() {
        if (isBatchActivationSlot()) {
            return Optional.of(((BatchActivationSlotValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PlacementData> getPlacement() {
        if (isPlacement()) {
            return Optional.of(((PlacementValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PushNotificationPlacementData> getPlacementPushNotification() {
        if (isPlacementPushNotification()) {
            return Optional.of(((PlacementPushNotificationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<EmailPlacementData> getPlacementEmail() {
        if (isPlacementEmail()) {
            return Optional.of(((PlacementEmailValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof IncludedResource && value.equals(((IncludedResource) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitContentStrategy(ContentStrategyInclusion contentStrategy);

        T visitBatchActivationSlot(BatchActivationSlotInclusion batchActivationSlot);

        T visitPlacement(PlacementData placement);

        T visitPlacementPushNotification(PushNotificationPlacementData placementPushNotification);

        T visitPlacementEmail(EmailPlacementData placementEmail);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(ContentStrategyValue.class),
        @JsonSubTypes.Type(BatchActivationSlotValue.class),
        @JsonSubTypes.Type(PlacementValue.class),
        @JsonSubTypes.Type(PlacementPushNotificationValue.class),
        @JsonSubTypes.Type(PlacementEmailValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("contentStrategy")
    @JsonIgnoreProperties("type")
    private static final class ContentStrategyValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private ContentStrategyInclusion value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ContentStrategyValue() {}

        private ContentStrategyValue(ContentStrategyInclusion value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitContentStrategy(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ContentStrategyValue && equalTo((ContentStrategyValue) other);
        }

        private boolean equalTo(ContentStrategyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IncludedResource{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("batchActivationSlot")
    @JsonIgnoreProperties("type")
    private static final class BatchActivationSlotValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private BatchActivationSlotInclusion value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BatchActivationSlotValue() {}

        private BatchActivationSlotValue(BatchActivationSlotInclusion value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBatchActivationSlot(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BatchActivationSlotValue && equalTo((BatchActivationSlotValue) other);
        }

        private boolean equalTo(BatchActivationSlotValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IncludedResource{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("placement")
    @JsonIgnoreProperties("type")
    private static final class PlacementValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private PlacementData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PlacementValue() {}

        private PlacementValue(PlacementData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPlacement(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PlacementValue && equalTo((PlacementValue) other);
        }

        private boolean equalTo(PlacementValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IncludedResource{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("placementPushNotification")
    @JsonIgnoreProperties("type")
    private static final class PlacementPushNotificationValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private PushNotificationPlacementData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PlacementPushNotificationValue() {}

        private PlacementPushNotificationValue(PushNotificationPlacementData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPlacementPushNotification(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PlacementPushNotificationValue && equalTo((PlacementPushNotificationValue) other);
        }

        private boolean equalTo(PlacementPushNotificationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IncludedResource{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("placementEmail")
    @JsonIgnoreProperties("type")
    private static final class PlacementEmailValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private EmailPlacementData value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PlacementEmailValue() {}

        private PlacementEmailValue(EmailPlacementData value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPlacementEmail(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PlacementEmailValue && equalTo((PlacementEmailValue) other);
        }

        private boolean equalTo(PlacementEmailValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IncludedResource{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IncludedResource{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
