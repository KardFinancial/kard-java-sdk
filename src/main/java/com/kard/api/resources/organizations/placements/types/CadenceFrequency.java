/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.kard.api.resources.organizations.placements.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CadenceFrequency {
    public static final CadenceFrequency DAILY = new CadenceFrequency(Value.DAILY, "DAILY");

    public static final CadenceFrequency MONTHLY = new CadenceFrequency(Value.MONTHLY, "MONTHLY");

    public static final CadenceFrequency WEEKLY = new CadenceFrequency(Value.WEEKLY, "WEEKLY");

    private final Value value;

    private final String string;

    CadenceFrequency(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CadenceFrequency && this.string.equals(((CadenceFrequency) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DAILY:
                return visitor.visitDaily();
            case MONTHLY:
                return visitor.visitMonthly();
            case WEEKLY:
                return visitor.visitWeekly();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CadenceFrequency valueOf(String value) {
        switch (value) {
            case "DAILY":
                return DAILY;
            case "MONTHLY":
                return MONTHLY;
            case "WEEKLY":
                return WEEKLY;
            default:
                return new CadenceFrequency(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        DAILY,

        WEEKLY,

        MONTHLY,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitDaily();

        T visitWeekly();

        T visitMonthly();

        T visitUnknown(String unknownType);
    }
}
